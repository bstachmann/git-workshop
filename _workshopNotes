# Aliase in `bash`

    alias myeditor="/usr/bin/vim"

Man kann die Zeile in die Datei `~/.bash_profile` eintragen, 
damit das Alias bei jedem Login gesetzt wird.

# Aliase in Git

    git config --global alias.l "log --oneline -8"

    git config --global alias.ci "commit -a"


# TODO

 * altes build.zip löschen
 * SourceTree Typo
 * git confi --global <missing param>